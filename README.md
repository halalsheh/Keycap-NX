for installation just copy the contents of keycap.7z into the root of your sd card.

if you have a system_config.ini file already in your /atmosphere/config folder make sure you add enable_hid_mitm = u8!0x1 in a new line in there.

tested on and developed for fw 18.1, don't count on updates.
